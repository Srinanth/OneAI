// Manages the state of the Home Screen's chat list,
// responsible for fetching all available chat sessions for the current user from the backend, handling loading states, and sorting them by date.
// probably sidebar support

// Manages the sidebar history. Fetches the list of chats from the backend. Contains the logic to 'renameChat(id, newTitle)
// and refresh the list when a new chat starts.